 This prototype scans around to find the box defined in the system and continues scanning
until it finds the right box. When the robot finds the right box, it takes the box 
autonomously then the robot goes into manual mode and robot takes it to the rack defined in the system.  